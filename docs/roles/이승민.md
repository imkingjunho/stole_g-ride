# 이승민 — 파트 ① 플랫폼·요청

> 이 파일은 AI 코딩 어시스턴트가 읽는 작업 지시서다. 완료한 항목은 `[x]`로 바꾼다. AI는 **가장 위의 미완료 항목**부터 진행한다.
> 근거 문서는 `docs/PRD.md`. 특히 §9(구조), §12.3-①(내 파트), §14(계약), §15.2(Phase 0)를 따른다.

## 내 파트 한 줄

팀 전체가 올라탈 **뼈대·계약·스텁·CI**를 Phase 0에 만들고, 그 뒤 **매칭 요청·대기열·만료** 모듈을 구현하며, 마지막에 **통합·성능·배포**를 맡는다. 이 파트의 절반은 "남이 막히지 않게 하는 일"이다.

## 내 폴더 (여기만 수정한다)

```
backend/src/main/java/com/gachiga/
├── GachigaApplication.java
├── common/        공통 응답·예외·에러 코드·유틸·ArchitectureTest
├── config/        Jackson, Redis, Swagger, CORS, 스케줄링
├── contract/      ⚠️ 동결 계약 (PRD §14) — 나만 수정, 요청 받아서
└── ride/          RideRequest, 요청 API, Redis 대기열, 만료 스케줄러, RideRequestPort·QueueStatusPort 구현
backend/src/main/resources/application*.yml, domain/ride.yml
backend/src/test/java/com/gachiga/{common,config,ride}/
backend/build.gradle, backend/settings.gradle
docker-compose.yml, .env.example, .gitignore, .github/**
docs/PRD.md, docs/api-spec.yaml, README.md, CLAUDE.md
```

**Phase 0 한정 예외:** `frontend/` 초기 스캐폴드와 다른 사람 폴더의 스텁 파일은 Phase 0에 내가 만든다. Phase 0 종료 공지 이후에는 그 폴더들을 건드리지 않는다.

## 담당 FR

FR-07(요청 필드), **FR-08**(대기열 등록/취소), FR-09(대기 상태 데이터 — `QueueStatusPort`), **FR-10**(만료 처리), E-02(낙관적 락), E-04(만료 30초 전 제외), E-08(최소 500m)

## 계약 (PRD §14)

| 내가 **구현**하는 것 | 내가 **쓰는** 것 |
|---|---|
| `RideRequestPort` (서준이 사용) | `RouteProvider`, `HubPort` (송준호) — 단독 거리·요금 캐시, 거점 검증 |
| `QueueStatusPort` (임승현이 사용) | `UserPort` (임승현) — 이용 제한 사용자 요청 차단 |
| 이벤트 발행: `RideRequestCreated / Cancelled / Expired` | 이벤트 수신: `GroupConfirmed / Dissolved / Completed` → 요청 상태 정리 |
| `common/`·`contract/` 전체 (전원이 사용) | |

---

## Phase 0 (9/21 ~ 9/27) — 뼈대 🔥 이 주는 이것만

> 순서가 중요하다. 0-1~0-7을 먼저 끝내 백엔드 3명을 출발시키고, 0-8~0-9로 프론트 2명을 출발시킨다.

- [x] **T0-0. Spring Boot 프로젝트·Docker Compose** — 이미 완료 (Java 17, Boot 3.2.5, MySQL 8 + Redis 7)

- [x] **T0-1. 브랜치·CI**
  - GitHub에서 `develop` 브랜치 삭제 (main만 남긴다) — 9/19 `git ls-remote` 확인: 원격에 `develop`이 없어 삭제 불필요
  - `.github/workflows/ci.yml`: `push`(main) 시 두 job — `backend`(`./gradlew build`), `frontend`(`npm ci && npm run lint && npm run build`). 프론트 job은 `frontend/` 생기기 전까지 건너뜀 — `detect-frontend` job이 체크아웃 후 `package.json` 존재를 판정 (job 레벨 `if`에서는 `hashFiles()`가 허용되지 않아 워크플로 검증 오류가 나므로 쓰지 않음)
  - README 상단에 CI 배지
  - 완료 기준: main에 push 하면 Actions가 돌고 초록/빨강이 보인다

- [x] **T0-2. `common/`**
  - `response/ApiResponse<T>`(`success, data, error`), `ApiError(code, message)`, 정적 팩토리 `ok(data)`, `fail(errorCode)`
  - `exception/ErrorCode` enum — PRD §8 목록 전부 (HTTP 상태 포함), `BusinessException(ErrorCode)`, `GlobalExceptionHandler`(`@RestControllerAdvice`: BusinessException, `MethodArgumentNotValidException` → `INVALID_INPUT`, 그 외 → `INTERNAL_ERROR`)
  - `util/GeoUtils.haversineMeters(lat1, lng1, lat2, lng2)`
  - 완료 기준: 테스트 컨트롤러로 세 종류 예외가 모두 `ApiResponse` 형태로 나온다

- [x] **T0-3. `contract/`** — PRD §14.1·§14.2를 **그대로** 코드로
  - `contract/route/`: `Coordinate`, `RouteResult(+Section)`, `RouteProvider`, `HubInfo`, `HubPort`
  - `contract/user/`: `Gender`, `UserStatus`, `UserSummary`, `UserPort`
  - `contract/ride/`: `WaitingRequest`, `RideRequestPort`, `QueueStatus`, `QueueStatusPort`
  - `contract/matching/`: `MatchHistoryPort`
  - `contract/auth/`: `@CurrentUser`
  - `contract/event/`: 8개 record
  - 각 타입에 Javadoc으로 의미·단위·null 여부를 적는다 (AI가 읽는다)

- [x] **T0-4. 스텁 전부** (PRD §14.7) — **미래 소유자의 폴더 안에** 만든다
  - `auth/DevSecurityConfig`(permitAll, CSRF off, CORS 허용) + `auth/DevCurrentUserResolver`(`X-Dev-User` 헤더, 기본 1L) + `auth/AuthWebMvcConfig`(리졸버 등록)
  - `user/StubUserAdapter implements UserPort` — id 1~4 (닉네임·성별 섞어서)
  - `route/EstimatedRouteProvider implements RouteProvider` — Haversine×1.3, 광주 택시 요금표(**2025-10-22 시행 실제 값 확인 반영: 기본 4,800원/1.7km + 100원/132m**. 시간 병산 32초/100원과 심야할증 23~24시 20%·0~2시 30%·2~4시 20%는 `findRoute`에 출발 시각이 없어 미반영), `estimated=true`, sections는 구간별 직선
  - `route/StaticHubAdapter implements HubPort` — 거점 6개(좌표는 OSM·위키 기반 임시값, 송준호가 실측으로 교체. 특히 예대 삼거리는 통칭이라 ±200~300m 오차 추정). `type`은 PRD §7.1 `hubs.type` ENUM 4종만 사용
  - `ride/InMemoryRideRequestAdapter implements RideRequestPort, QueueStatusPort` — `ConcurrentHashMap`
  - `matching/StubMatchHistoryAdapter implements MatchHistoryPort` — 항상 true / 빈 리스트
  - `realtime/WebSocketConfig` — `/ws` 엔드포인트, `/app` prefix, `/topic`·`/queue` simple broker, `/user` prefix. 인증 없음
  - 완료 기준: `./gradlew bootRun` 성공, 컨트롤러 없는 상태에서도 컨텍스트가 뜬다

- [x] **T0-5. `config/`·설정 분리**
  - `JacksonConfig`(Asia/Seoul, ISO 날짜), `RedisConfig`(`StringRedisTemplate`), `SwaggerConfig`(제목·Bearer 인증 헤더 UI), `CorsConfig`(localhost:5173), `SchedulingConfig`
  - `application.yml`에 `spring.config.import: optional:classpath:domain/ride.yml, optional:classpath:domain/matching.yml, optional:classpath:domain/auth.yml, optional:classpath:domain/route.yml` + 빈 파일 4개 생성
  - `application.yml` `ddl-auto: update` (local·test), `application-prod.yml` `validate`
  - test 프로파일: 외부 DB 없이 `./gradlew build` 통과 (T1-2 에서 임베디드 H2 로 전환 — 리포지토리 빈이 필요해졌다)
  - `build.gradle`: `spring-boot-starter-mail`, `jjwt`(0.12.x), `archunit-junit5`, `jacoco` 추가

- [x] **T0-6. `common/ArchitectureTest`** (ArchUnit)
  - 규칙: `com.gachiga.{ride,matching,fare,stats,auth,user,realtime,route}` 각 패키지는 **다른 도메인 패키지를 import 하지 않는다** (`contract`, `common`, `config`는 허용)
  - 규칙: 엔티티(`@Entity`)는 `@ManyToOne`/`@OneToMany` 등으로 다른 패키지 엔티티를 참조하지 않는다
  - 완료 기준: 일부러 `matching`에서 `ride` 클래스를 import 하면 `./gradlew build`가 실패한다 (확인 후 되돌리기)

- [x] **T0-7. `docs/api-spec.yaml` v1** — PRD §8 전체 엔드포인트의 요청·응답 스키마 (OpenAPI 3.0). `ApiResponse` 래퍼 포함. 그룹 상세는 §14.4 예시 그대로. **여기까지 끝나면 단톡방에 "백엔드 시작하세요" 공지**

- [x] **T0-8. `frontend/` 스캐폴드**
  - Vite + React 18 + TypeScript, Tailwind, React Router, TanStack Query, Zustand, `openapi-fetch`, `openapi-typescript`(`npm run gen:api`), MSW, `@stomp/stompjs`, `react-kakao-maps-sdk`, ESLint + Prettier(`npm run lint`)
  - PRD §14.6 라우트 전부 + `features/{auth,request,map,group,chat,history}/` placeholder 페이지(제목만)
  - `shared/api/client.ts`, `shared/stores/authStore.ts`, `shared/ws/stompClient.ts`(mock 모드 포함), `shared/mocks/handlers.ts`(api-spec 전체 happy path), `shared/ui/`(빈 Button 하나)
  - `.env.example`에 `VITE_API_MODE=mock`, `VITE_API_BASE_URL`, `VITE_KAKAO_JS_KEY` 추가
  - 완료 기준: `npm run dev`로 PRD §3.2 시나리오를 목 데이터로 끝까지 클릭. `npm run build`·`lint` 통과. **"프론트 시작하세요" 공지**

- [x] **T0-9. 마무리** — README·CLAUDE.md·`docs/roles/*.md`가 실제 폴더 구조와 맞는지 확인, 전원에게 clone → M0 재현 요청, 단톡방에 "Phase 0 끝"
  - 9/19 깨끗한 clone 에서 M0 재현 확인: 백엔드 `./gradlew build` 통과(DB 없이), 프론트 `npm ci && lint && build` 통과, `npm run dev` 로 6개 화면 클릭, CI run #6 백엔드·프론트 모두 초록

## Phase 1 (9/28 ~ 10/25) — P0: `ride` 모듈 실구현

- [x] **T1-1. `RideRequest` 엔티티·리포지토리** — PRD §7 `ride_requests` 그대로. `@Version`. `userId`·`hubId`는 `Long`(ID 참조). status enum 6종
- [x] **T1-2. 요청 생성 API** — `POST /api/requests` (FR-07, FR-08)
  - 검증: 진행 중(WAITING/MATCHED/CONFIRMED) 요청 1건 제한 → `ALREADY_IN_QUEUE`; `HubPort`로 거점 확인; 거점~목적지 500m 미만 → `REQUEST_TOO_SHORT`(E-08); `UserPort`로 SUSPENDED 차단
  - `RouteProvider.findRoute(hub, [], dest)`로 `soloDistance`·`soloFare` 캐시
  - `expiresAt = now + maxWaitMin`; `RideRequestCreated` 발행
- [x] **T1-3. 취소·조회 API** — `DELETE /api/requests/{id}`(본인만, 대기열 즉시 제거, `RideRequestCancelled`), `GET /api/requests/me`(진행 중 요청 + `QueueStatus` 포함)
- [x] **T1-4. Redis 대기열** — `queue:{hubId}` Sorted Set(score=departAt epoch). 등록·제거·조회. DB 상태와 항상 일치(트랜잭션 커밋 후 Redis 반영)
- [x] **T1-5. `RideRequestPort` 실구현** — `findWaiting()`(WAITING & expiresAt > now+30s), `tryMarkMatched()`(`@Version` 충돌 시 false), `markWaiting/Confirmed/Completed`. `InMemoryRideRequestAdapter` 삭제
- [x] **T1-6. `QueueStatusPort` 실구현** — `remainingSeconds`, `candidateCount`(같은 거점 WAITING 수 − 1)
- [ ] **T1-7. 만료 스케줄러** — 30초 주기, `expiresAt` 경과 WAITING → EXPIRED, 대기열 제거, `RideRequestExpired` 발행 (FR-10)
- [ ] **T1-8. 그룹 이벤트 수신** — `GroupConfirmed`→CONFIRMED, `GroupDissolved`→WAITING(재대기열, 남은 시간 유지), `GroupCompleted`→COMPLETED
- [ ] **T1-9. 테스트** — 요청 1건 제한, 500m 차단, 낙관적 락 충돌, 만료 처리. `ride` 패키지 테스트는 인메모리(Mock) 기준

## Phase 2 (10/26 ~ 11/22) — P1·안정화

- [ ] **T2-1. 데모 시뮬레이터** — `local` 프로파일 전용 `POST /api/dev/simulate?hubId=&count=` 가상 요청 N건 투입(스텁 사용자 id 사용). 시연·튜닝용
- [ ] **T2-2. 통합 점검** — 모든 엔드포인트를 `docs/api-spec.yaml` ↔ Swagger `/v3/api-docs`와 대조, 불일치는 구현자에게 통보
- [ ] **T2-3. N+1 제거** — 쿼리 로그로 확인, `@EntityGraph`/fetch join. `open-in-view: false` 유지
- [ ] **T2-4. 계약 변경 요청 처리** — 단톡방 요청을 24시간 내 반영·공지 (상시)

## Phase 3 (11/23 ~ 12/13) — 성능·배포

- [ ] **T3-1. k6 부하 테스트** — 동시 200명, 요청 생성·조회 시나리오, 목표 P95 < 500ms. 전후 수치 기록
- [ ] **T3-2. 스키마 고정** — 로컬 DB에서 `docs/schema.sql` 추출, `prod` 프로파일 `validate`로 베타 서버 기동 확인
- [ ] **T3-3. 베타 배포** — 서버 1대(또는 팀원 노트북 + 터널)에 docker compose + jar + 정적 프론트. HTTPS. 카카오 콘솔 도메인 등록 확인
- [ ] **T3-4. `docs/performance.md`** — 개선 전후 수치 (포트폴리오 자료)

---

## 이 파트만으로 보여줄 수 있는 것

Swagger에서 요청 생성 → Redis에 `queue:{hubId}` 항목 → `GET /api/requests/me`에 남은 시간 → 만료 후 EXPIRED. 스텁 사용자·스텁 경로로도 전부 동작한다.

## 완료 기준 (DoD) — 매 작업마다

- [ ] `cd backend && ./gradlew build` 통과 (ArchUnit 포함)
- [ ] Swagger에 API 노출, `docs/api-spec.yaml`과 일치
- [ ] 단위 테스트 있음
- [ ] 이 파일의 체크박스 갱신 → PRD §13.4 루틴으로 main에 push

## 포트폴리오 포인트

> "5인 팀이 브랜치·PR 없이 main 하나에 직접 push 하면서도 빌드가 깨지지 않도록 **모듈러 모놀리스 구조(계약 패키지·ID 참조·이벤트·ArchUnit 경계 검사)**를 설계하고 Phase 0 뼈대를 구축. Redis Sorted Set 대기열과 스케줄러로 30초 주기 배치 매칭 파이프라인의 요청 측을 구현하고, k6로 P95 500ms 달성."

- 왜 PR 대신 폴더 소유제 + 계약 동결 + CI를 택했는지 (팀 상황에 맞는 프로세스 설계)
- 낙관적 락으로 중복 배정을 막은 방법 (E-02)
- T3-1 전후 수치는 반드시 남길 것
